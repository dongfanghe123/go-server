<?xml version="1.0" encoding="UTF-8" ?>
<!-- 这里的 namespace 必须和 Go 中定义的 UserRepository 接口完整路径一字不差 -->
<mapper namespace="go-server.dao.VoucherDao">

    <insert id="SaveVoucher" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tb_voucher
        (
        shop_id,
        title,
        sub_title,
        rules,
        pay_value,
        actual_value,
        type,
        status,
        create_time,
        update_time
        )
        VALUES
        (
        #{shopId},
        #{title},
        #{subTitle},
        #{rules},
        #{payValue},
        #{actualValue},
        #{type},
        #{status},
        #{createTime},
        #{updateTime}
        )
    </insert>


    <insert id="SaveVoucherSeckill">
        INSERT INTO tb_seckill_voucher
        (
        voucher_id,
        stock,
        create_time,
        begin_time,
        end_time,
        update_time
        )
        VALUES
        (
        #{voucherId},
        #{stock},
        #{createTime},
        #{beginTime},
        #{endTime},
        #{updateTime}
        )

    </insert>


</mapper>